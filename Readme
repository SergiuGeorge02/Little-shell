This is an implementation of a shell that works similar with the linux one.
Commands that were implemented by myself are: cd, help, version rm(with parameters: -i, -r (-R), -v) uniq(with parameters:-i, -d, -u) and dir(no parameters were implemented) 
